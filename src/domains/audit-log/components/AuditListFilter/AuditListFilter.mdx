import { Canvas, Meta, Controls } from '@storybook/blocks';

import AuditListFilter, { Default } from './AuditListFilter.stories';

<Meta of={AuditListFilter} />


#### Design: <a href="https://www.figma.com" target="_blank" rel="noopener">Figma</a>

# AuditListFilter

A filter UI component designed to filter audit records based on multiple criteria. It allows users to select a date range (fromDate and toDate), 
filter by entity ID and identity ID using searchable input fields, and filter by success status with a dropdown select. 

<Canvas of={Default} />

## Properties

<Controls of={Default} />

## Examples

### Default
<Canvas of={Default} />

## CSS Source

<details>
<summary>View CSS Source</summary>

```css
.container {
  display: flex;
  justify-content: space-between;
  gap: var(--spacing-xs);
  width: 100%;
  flex-wrap: wrap;
  align-items: flex-start;
}

.item {
  flex: 1 1 calc(var(--spacing-l) * 5);
  min-width: calc(var(--spacing-l) * 5);
  display: flex;
  flex-direction: column;
  position: relative;
}

.item-success {
  flex: 0 0 10%;
  min-width: calc(var(--spacing-s) * 4);
  display: flex;
  flex-direction: column;
}

```
</details>