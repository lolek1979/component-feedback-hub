import { Canvas, Meta, Controls } from '@storybook/blocks';

import QuerySelect, { Default, EmptyResult} from './QuerySelect.stories';

<Meta of={QuerySelect} />


#### Design: <a href="https://www.figma.com" target="_blank" rel="noopener">Figma</a>

# QuerySelect

QuerySelect component allows users to search and select items from a dropdown list.

<Canvas of={Default} />

## Properties

<Controls of={Default} />

## Examples

### Default
<Canvas of={Default} />

### EmptyResult
<Canvas of={EmptyResult} />

## CSS Source

<details>
<summary>View CSS Source</summary>

```css
.container {
  position: relative;
  width: 100%;

  .input {
    width: 100%;
  }

  .dropdown-list {
    max-height: calc(var(--spacing-3xl) * 2);
    overflow-y: auto;
    border: var(--spacing-5xs) solid var(--palette-grey-200);
    background: var(--palette-grey-50);
    box-shadow: 0 var(--spacing-5xs) var(--spacing-3xs) var(--palette-alpha-100);
    border-radius: var(--spacing-4xs);
    position: absolute;
    width: 100%;
    margin-top: var(--spacing-4xs);
    z-index: 50;
  }

  .empty-result {
    padding: var(--spacing-3xs);
    font-size: var(--size-14);
    color: var(--palette-grey-100);
  }

  .dropdown-item {
    cursor: pointer;
    padding-left: var(--spacing-2xs);
    padding-right: var(--spacing-2xs);
    padding-top: var(--spacing-3xs);
    padding-bottom: var(--spacing-3xs);
    font-size: var(--size-14);
  }

  .dropdown-item:hover {
    background-color: var(--palette-alpha-50);
  }
}

:global(.dark) .container {
  .dropdown-list {
    background: var(--palette-grey-950);
    border-color: var(--palette-grey-800);
  }

  .empty-result {
    color: var(--palette-grey-900);
  }

  .dropdown-item {
    color: var(--palette-grey-0);
  }

  .dropdown-item:hover {
    background-color: var(--palette-alpha-600);
  }

  .dropdown-list::-webkit-scrollbar {
    background-color: var(--palette-grey-950);
  }

  .dropdown-list::-webkit-scrollbar-thumb {
    background: var(--palette-grey-600);
  }

  .dropdown-list::-webkit-scrollbar-thumb:hover {
    background: var(--palette-grey-500);
  }
}

```
</details>