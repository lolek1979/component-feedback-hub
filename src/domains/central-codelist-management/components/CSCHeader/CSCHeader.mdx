import { Canvas, Meta, Controls } from '@storybook/blocks';

import CSCHeader, { Default, ActiveStatus } from './CSCHeader.stories';

<Meta of={CSCHeader} />

#### Design: <a href="https://www.figma.com/design/X6VaqX3yF7GOyaVg2qLbdY/MVP-design?node-id=3074-10545&t=DYHGcY4n5yLpdtST-4" target="_blank" rel="noopener">Figma</a>

# CSCHeader

The `CSCHeader` component is used as the main header for pages, featuring breadcrumb navigation, a title with optional status badge, and action buttons.

<Canvas of={Default} />

## Properties

<Controls of={Default} />

## Examples

### Default Header with Status
<Canvas of={Default} />

### Header with Active Status
<Canvas of={ActiveStatus} />


## CSS Source

<details>
<summary>View CSS Source</summary>

```css
.header {
  padding: 32px;

  .navigation {
    margin-bottom: 8px;
  }

  .delete-text {
    color: var(--support-negative);

    path {
      fill: var(--support-negative);
    }
  }

  .main-content {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 24px;

    .title-container {
      display: flex;
      align-items: flex-start;
      gap: 16px;
      flex: 1;
      min-width: 0;
    }

    .actions {
      display: flex;
      gap: var(--spacing-2xs);
      align-items: flex-start;
      flex-wrap: wrap;
      justify-content: flex-end;
    }
  }

  .parent-info {
    margin-top: 4px;
    display: flex;
    justify-content: flex-start;
    align-items: flex-end;
    gap: 8px;
  }

  @media (max-width: 1200px) {
    .main-content {
      .actions {
        gap: 8px;
      }
    }
  }

  @media (max-width: 900px) {
    padding: 24px;

    .main-content {
      flex-direction: column;
      gap: 16px;

      .title-container {
        width: 100%;
      }

      .actions {
        width: 100%;
        justify-content: flex-start;

        & :global(.button) {
          flex: 1;
          min-width: 140px;
          max-width: calc(50% - 4px);
        }
      }
    }
  }

  @media (max-width: 480px) {
    padding: 16px;

    .main-content {
      .actions {
        & :global(.button) {
          max-width: 100%;
        }
      }
    }
  }
}

:global(.dark) .header {
  .parent-info {
    svg path {
      fill: white;
    }

    .parent-link {
      color: var(--palette-red-400) !important;
    }
  }
}

```
</details>