import { Canvas, Meta, Controls } from '@storybook/blocks';

import Stepper,{Default} from './Stepper.stories';

<Meta of={Stepper} />

#### Design: <a href="https://www.figma.com/design/vWA0LlPduDiPJvjPjDCvI3/Design-system-0.1?node-id=2202-28778&t=2yHWLITxw72bpVPe-4" target="_blank" rel="noopener">Figma</a>

# Stepper

The Stepper component displays a sequence of steps in a process, with options for sub-steps, completed states, and disabled states.

<Canvas of={Default} />

## Properties

<Controls of={Default} />

## Examples

### Default
<Canvas of={Default} />

## CSS Source

<details>
<summary>View CSS Source</summary>

```css
.stepper {
  --step-width: 360px;
  --step-height: 56px;

  display: flex;
  max-width: 500px;
  padding-top: var(--Spacing-M, var(--spacing-m));
  flex-direction: column;
  align-items: flex-start;
  flex-shrink: 0;
  position: relative;
  width: var(--step-width);

  .stepper-content {
    width: auto;
    flex: 1;
  }

  .stepper-controls {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    display: flex;
    gap: var(--units-10);
    justify-content: center;
    padding: var(--spacing-xs) 0;
    background-color: var(--background-base);
  }

  .stepper.vertical {
    flex-direction: column;
  }

  .stepper.horizontal {
    flex-direction: row;
  }

  .step-group {
    margin-bottom: var(--spacing-3xs);
  }

  .step {
    display: flex;
    align-items: flex-start;
    cursor: default;
    border-radius: var(--radius-2xs);
    transition: background-color 0.3s ease;
    min-height: var(--step-height);
    min-width: var(--step-width);
  }

  .step.expandable {
    cursor: pointer;
  }

  .step.active {
    font-weight: bold;
  }

  .step.disabled {
    color: var(--text-disabled);
    cursor: not-allowed;
    pointer-events: none;
  }

  .step-icon-container {
    margin-right: var(--spacing-4xs);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .badge-container {
    margin-left: auto;
  }

  .step-icon {
    width: auto;
    height: var(--spacing-s);
    display: flex;
    align-items: center;
    justify-content: center;
    color: inherit;
  }

  .step.active .step-icon {
    background-color: transparent;
  }

  .step.completed .step-icon {
    background-color: transparent;
  }

  .step-label-container {
    flex: 1;
    display: flex;
    align-items: center;
    min-height: 48px;
    padding: var(--units-12) 0;
    overflow: visible;
  }

  .step-label {
    margin-right: var(--units-8);
  }
}

```
</details>