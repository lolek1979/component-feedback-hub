import { Meta, Story, Canvas, Controls } from '@storybook/addon-docs/blocks';
import ToggleContent, {
  Default,
  WithStyling,
  Expanded,
  Disabled,
  WithDefaultBadgeLabels,
  WithCustomBadgeLabels,
  BadgesInStepperContext,
} from './ToggleContent.stories';

<Meta of={ToggleContent} />
# ToggleContent

A component that allows content to be shown or hidden with a toggle action.

<Canvas of={Default} />
## Examples
<Controls of={Default} />

### Styled Toggle Content

<Canvas of={WithStyling} />

### Expanded

<Canvas of={Expanded} />

### Disabled

<Canvas of={Disabled} />

### WithDefaultBadgeLabels

<Canvas of={WithDefaultBadgeLabels} />
### WithCustomBadgeLabels

<Canvas of={WithCustomBadgeLabels} />
### BadgesInStepperContext

<Canvas of={BadgesInStepperContext} />

## CSS Source

<details>
<summary>View CSS Source</summary>

```css
.toggleContentWrapper {
  .toggleContentContainer {
    border: none;
    border-radius: var(--radius-xs);
    margin: var(--spacing-3xs) 0;
    overflow: hidden;
    min-height: 36px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .toggleHeader {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: var(--spacing-3xs);
    align-self: stretch;
    height: 56px;
    cursor: pointer;
    border-radius: var(--radius-xs);
    background: transparent;
    user-select: none;
    position: relative;
  }

  .toggleContentRow {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    gap: var(--spacing-s);
  }

  .toggleTitleContainer {
    flex-grow: 0;
    flex-shrink: 1;
    min-width: 0;
    max-width: 70%;
    width: 242px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .statusBadgeContainer {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: var(--units-10);
    position: static;
    margin-left: auto;
  }

  .statusBadge {
    font-size: var(--size-12);
    padding: var(--units-2) var(--units-8);
    border-radius: var(--radius-m);
    margin-left: var(--spacing-4xs);
    margin-right: var(--spacing-4xs);
    text-transform: capitalize;
    font-weight: var(--font-weight-500);
    display: inline-block;
    line-height: var(--line-height-18);
  }

  .toggleTitle {
    color: var(--text-primary);
    font-size: var(--size-16);
    font-style: normal;
    font-weight: 600;
    line-height: var(--line-height-20);
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    width: 100%;
  }

  .toggleButton {
    display: flex;
    width: var(--units-24);
    height: var(--units-24);
    justify-content: center;
    align-items: center;
    background: none;
    border: none;
    cursor: pointer;
    position: static;
    margin-right: 0;
    flex-shrink: 0;
  }

  .rightContainer {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    flex-shrink: 0;
  }

  .toggleHeader:hover .toggleTitle {
    font-weight: 600;
  }

  .activeHeader .toggleTitle {
    font-weight: 600 !important;
  }

  .toggleBody {
    background: white;
    overflow: hidden;
    width: 360px;
    transition-property: max-height, opacity, padding;
    transition-duration: 0.3s, 0.3s, 0.3s;
    transition-timing-function: ease-in-out, ease-in-out, ease-in-out;
    padding: var(--units-8) var(--units-16);
  }

  .collapsed {
    max-height: 0;
    opacity: 0;
    padding-top: 0;
    padding-bottom: 0;
    margin: 0;
    transition-delay: 0s, 0.15s, 0s;
  }

  .expanded {
    max-height: 1000px;
    opacity: 1;
    transition-delay: 0.15s, 0s, 0.15s;
  }

  .toggleContentContainer.disabled .toggleHeader {
    cursor: default;
  }

  .toggleContentContainer.disabled,
  .toggleContentContainer.disabled * {
    color: var(--text-disabled) !important;
  }

  .toggleContentContainer.disabled .toggleTitle {
    color: var(--text-disabled) !important;
    font-weight: 400 !important;
  }

  .toggleContentContainer.disabled .toggleBody {
    color: var(--text-disabled) !important;
  }

  :global(.dark) & {
    .toggleBody {
      background: var(--palette-grey-900);
      color: var(--palette-grey-200);
    }

    .toggleTitle {
      color: var(--text-on-color) !important;
    }

    .toggleButton svg {
      fill: var(--palette-grey-800);
      color: var(--palette-grey-800);
    }

    .toggleButton svg * {
      fill: var(--palette-grey-200) !important;
      color: var(--palette-grey-200) !important;
    }

    .toggleHeader {
      background-color: transparent;
    }

    .toggleHeader:hover .toggleTitle {
      color: var(--palette-grey-0) !important;
    }

    .toggleContentContainer.disabled,
    .toggleContentContainer.disabled * {
      color: var(--palette-grey-600) !important;
    }

    .toggleContentContainer.disabled .toggleTitle {
      color: var(--palette-grey-600) !important;
    }

    .toggleContentContainer.disabled .toggleBody {
      color: var(--palette-grey-600) !important;
    }

    .activeHeader .toggleTitle {
      color: var(--palette-grey-0) !important;
      font-weight: var(--font-weight-600) !important;
    }

    .statusBadgeFinished {
      background-color: var(--palette-blue-950);
      color: var(--palette-blue-300);
    }

    .statusBadgeOpened {
      background-color: var(--palette-green-950);
      color: var(--palette-green-400);
    }

    .statusBadgePending {
      background-color: var(--palette-orange-1000);
      color: var(--palette-orange-400);
    }

    .statusBadgeError {
      background-color: var(--palette-red-950);
      color: var(--palette-red-300);
    }
  }
}

```
</details>